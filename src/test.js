// Import the function to be tested
const { TennisScoreCalculator } = require('./printScore');

// Call the function with the desired input
TennisScoreCalculator('Score Match 02');
TennisScoreCalculator('Games Player Person A');

